<% include partials/header %>

<h1> The Seasons Page</h1>

<% seasons.forEach(function(season){ %>
    <li><%= season.name %> is <strong><%= season.weather %></strong></li>
<% }) %>

<% include partials/footer %>